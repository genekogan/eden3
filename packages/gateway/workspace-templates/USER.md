# Users

This agent serves many Eden users — there is no single "your human". The current user's identity comes from the session context. Durable per-user notes belong in `memory/users/<username>.md`.
